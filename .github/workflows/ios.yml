name: iOS build

# Pushes and pull requests only build. Manual runs can also upload to Apple.
on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:
    inputs:
      upload_to_app_store_connect:
        description: Upload a signed IPA to App Store Connect without submitting for review
        required: true
        default: false
        type: boolean

# A newer run replaces any in-progress iOS run for the same Git ref.
concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # This job needs no Apple signing secrets. Its output runs in the simulator,
  # not on a physical iPhone or through TestFlight.
  build:
    name: Build unsigned iOS app
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      - name: Check out source code
        uses: actions/checkout@v6

      - name: Use Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      # Bundler installs and caches the Ruby tools, including CocoaPods and xcpretty.
      - name: Use Ruby with Bundler cache
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler: '4.0.16'
          bundler-cache: true

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Run JavaScript checks
        run: npm run verify

      # CocoaPods installs the native dependencies used by the Xcode workspace.
      - name: Install iOS dependencies
        working-directory: ios
        run: bundle exec pod install

      - name: Show Xcode version
        run: xcodebuild -version

      # Compile a Release build without signing. pipefail preserves Xcode failures
      # even though xcpretty formats the build output.
      - name: Build for the iOS Simulator
        run: |
          set -o pipefail
          xcodebuild \
            -workspace ios/CrossPlatformIOS.xcworkspace \
            -scheme CrossPlatformIOS \
            -configuration Release \
            -sdk iphonesimulator \
            -derivedDataPath ios/build \
            CODE_SIGNING_ALLOWED=NO \
            build | bundle exec xcpretty

      # Zip the .app bundle so it can be downloaded intact from the workflow run.
      - name: Package compiled application
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            ios/build/Build/Products/Release-iphonesimulator/CrossPlatformIOS.app \
            CrossPlatformIOS-simulator.zip

      - name: Publish compiled application
        uses: actions/upload-artifact@v4
        with:
          name: CrossPlatformIOS-simulator-${{ github.sha }}
          path: CrossPlatformIOS-simulator.zip
          if-no-files-found: error
          retention-days: 14

  # Only an explicitly requested manual release reaches this job, after build passes.
  release:
    name: Sign and upload to App Store Connect
    if: github.event_name == 'workflow_dispatch' && inputs.upload_to_app_store_connect
    needs: build
    runs-on: macos-latest
    timeout-minutes: 45
    # Use secrets and any approval rules configured on the app-store environment.
    environment: app-store

    steps:
      - name: Check out source code
        uses: actions/checkout@v6

      - name: Use Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Use Ruby with Bundler cache
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler: '4.0.16'
          bundler-cache: true

      # This job uses a fresh runner, so it must install its own dependencies.
      - name: Install dependencies
        run: |
          npm ci
          cd ios
          bundle exec pod install

      # Stop before signing if a required secret is missing. Never print its value.
      - name: Validate Apple configuration
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          IOS_BUNDLE_IDENTIFIER: ${{ secrets.IOS_BUNDLE_IDENTIFIER }}
          IOS_PROVISIONING_PROFILE_NAME: ${{ secrets.IOS_PROVISIONING_PROFILE_NAME }}
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          required=(
            APPLE_CERTIFICATE_BASE64
            APPLE_CERTIFICATE_PASSWORD
            APPLE_PROVISIONING_PROFILE_BASE64
            APPLE_TEAM_ID
            IOS_BUNDLE_IDENTIFIER
            IOS_PROVISIONING_PROFILE_NAME
            APP_STORE_CONNECT_API_KEY_ID
            APP_STORE_CONNECT_ISSUER_ID
            APP_STORE_CONNECT_API_KEY_BASE64
          )
          for name in "${required[@]}"; do
            if [[ -z "${!name}" ]]; then
              echo "Missing required app-store environment secret: $name" >&2
              exit 1
            fi
          done

      # Decode the signing files and import the certificate into a temporary keychain.
      # The provisioning profile ties the app to the Apple team and signing certificate.
      - name: Install signing certificate and profile
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_PROVISIONING_PROFILE_BASE64: ${{ secrets.APPLE_PROVISIONING_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          CERTIFICATE_PATH="$RUNNER_TEMP/distribution.p12"
          PROFILE_PATH="$RUNNER_TEMP/profile.mobileprovision"
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"

          echo -n "$APPLE_CERTIFICATE_BASE64" | base64 --decode > "$CERTIFICATE_PATH"
          echo -n "$APPLE_PROVISIONING_PROFILE_BASE64" | base64 --decode > "$PROFILE_PATH"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERTIFICATE_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          security list-keychain -d user -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          cp "$PROFILE_PATH" "$HOME/Library/MobileDevice/Provisioning Profiles/build.mobileprovision"

      # Build a separate device archive with manual signing. The workflow run number
      # becomes the app's build number. Rerunning the same run keeps that number.
      - name: Archive signed application
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          IOS_BUNDLE_IDENTIFIER: ${{ secrets.IOS_BUNDLE_IDENTIFIER }}
          IOS_PROVISIONING_PROFILE_NAME: ${{ secrets.IOS_PROVISIONING_PROFILE_NAME }}
        run: |
          set -o pipefail
          xcodebuild \
            -workspace ios/CrossPlatformIOS.xcworkspace \
            -scheme CrossPlatformIOS \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$RUNNER_TEMP/CrossPlatformIOS.xcarchive" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            PRODUCT_BUNDLE_IDENTIFIER="$IOS_BUNDLE_IDENTIFIER" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="$IOS_PROVISIONING_PROFILE_NAME" \
            CURRENT_PROJECT_VERSION="${{ github.run_number }}" \
            archive

      # Export the archive as an App Store IPA. The plist maps the bundle ID to
      # the provisioning profile Xcode must use for signing.
      - name: Export signed IPA
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          IOS_BUNDLE_IDENTIFIER: ${{ secrets.IOS_BUNDLE_IDENTIFIER }}
          IOS_PROVISIONING_PROFILE_NAME: ${{ secrets.IOS_PROVISIONING_PROFILE_NAME }}
        run: |
          EXPORT_OPTIONS="$RUNNER_TEMP/ExportOptions.plist"
          plutil -create xml1 "$EXPORT_OPTIONS"
          plutil -insert method -string app-store-connect "$EXPORT_OPTIONS"
          plutil -insert destination -string export "$EXPORT_OPTIONS"
          plutil -insert signingStyle -string manual "$EXPORT_OPTIONS"
          plutil -insert teamID -string "$APPLE_TEAM_ID" "$EXPORT_OPTIONS"
          plutil -insert provisioningProfiles -json '{}' "$EXPORT_OPTIONS"
          /usr/libexec/PlistBuddy \
            -c "Add :provisioningProfiles:$IOS_BUNDLE_IDENTIFIER string $IOS_PROVISIONING_PROFILE_NAME" \
            "$EXPORT_OPTIONS"

          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/CrossPlatformIOS.xcarchive" \
            -exportPath "$RUNNER_TEMP/export" \
            -exportOptionsPlist "$EXPORT_OPTIONS"

      # Keep a downloadable copy in GitHub Actions before attempting the Apple upload.
      - name: Publish signed IPA
        uses: actions/upload-artifact@v4
        with:
          name: CrossPlatformIOS-AppStoreConnect-${{ github.run_number }}
          path: ${{ runner.temp }}/export/CrossPlatformIOS.ipa
          if-no-files-found: error
          retention-days: 14

      # Apple processes this build for TestFlight or App Store submission.
      # Uploading does not submit it for review or publish it on the App Store.
      - name: Upload IPA to App Store Connect
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          PRIVATE_KEYS_DIR="$HOME/.appstoreconnect/private_keys"
          mkdir -p "$PRIVATE_KEYS_DIR"
          echo -n "$APP_STORE_CONNECT_API_KEY_BASE64" | base64 --decode \
            > "$PRIVATE_KEYS_DIR/AuthKey_$APP_STORE_CONNECT_API_KEY_ID.p8"

          xcrun altool --upload-app \
            --type ios \
            --file "$RUNNER_TEMP/export/CrossPlatformIOS.ipa" \
            --apiKey "$APP_STORE_CONNECT_API_KEY_ID" \
            --apiIssuer "$APP_STORE_CONNECT_ISSUER_ID"

      # Attempt cleanup even if signing, export, or upload fails.
      - name: Remove signing material
        if: always()
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
        run: |
          security delete-keychain "$RUNNER_TEMP/app-signing.keychain-db" || true
          rm -f "$RUNNER_TEMP/distribution.p12"
          rm -f "$RUNNER_TEMP/profile.mobileprovision"
          rm -f "$HOME/Library/MobileDevice/Provisioning Profiles/build.mobileprovision"
          rm -f "$HOME/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_API_KEY_ID.p8"
